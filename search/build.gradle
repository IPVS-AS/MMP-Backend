group = 'de.unistuttgart.ipvs.as.mmp.search'
version = '0.0.1-SNAPSHOT'
sourceCompatibility = 1.8

dependencies {
    compile project(':common'), project(":model")
}

processTestResources {
    exclude 'indexpath/**'
}